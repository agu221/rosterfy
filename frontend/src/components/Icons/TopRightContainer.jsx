import styles from '../../styles/icons/TopRightContainer.module.css'

const TopRightContainer = ({ children }) => {
    return <div className={styles.topRight}>{children}</div>;
};

export default TopRightContainer;
